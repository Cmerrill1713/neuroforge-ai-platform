# Core FastAPI dependencies
fastapi==0.104.1
uvicorn[standard]==0.24.0
gunicorn==22.0.0

# Database dependencies
sqlalchemy==2.0.23
psycopg2-binary==2.9.9
alembic==1.12.1

# Caching and rate limiting
redis==5.0.1
slowapi==0.1.9

# Authentication and security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.18

# Monitoring and logging
sentry-sdk[fastapi]==1.38.0
prometheus-client==0.19.0
psutil==5.9.6

# Data validation and configuration
pydantic[email]==2.5.0
python-dotenv==1.0.0

# AI/ML dependencies (without MLX for Docker)
torch==2.8.0
transformers==4.53.0
ollama==0.1.7
edge-tts==6.1.9
torchaudio==2.1.0
# chatterbox-tts==0.1.0  # Commented out for Docker compatibility
# mlx==0.8.0  # Apple Silicon only
# mlx-lm==0.8.0  # Apple Silicon only
